# Usage: One line per group. SSH keys must contain private keys. Password files must contain the password
# For linux with SSH key:	<groupName> linux ssh <username> <privateKeyFilename>
# For linux with password:	<groupName> linux password <username> <privateKeyFilename>
# For windows:              <groupName> windows <username> <passwordFilename>
